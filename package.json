{
  "name": "wiki",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs",
    "dev": "vitepress dev docs",
    "deploy": "git add dist && git commit -m 'Deploy dist' && git subtree push --prefix dist origin gh-pages --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "flexsearch": "^0.7.31",
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
